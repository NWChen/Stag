path = Path()


'''
from Tkinter import *

root = Tk()
label = Label(root, text="1234 Hello World")
label.pack()

root.mainloop()

from Tkinter import *

class App:
	def __init__(self, master):
		frame = Frame(master)
		frame.pack()

		self.button = Button(
			frame, text="Add segment", fg="black", command=frame.quit
		)
		self.button.pack(side=LEFT)

		self.hi_there = Button(frame, text="Hello", command=self.say_hi)
		self.hi_there.pack(side=LEFT)

	def say_hi(self):
		print "U R A SUCK"

root = Tk()

app = App(root)
root.mainloop()
root.destroy()
'''